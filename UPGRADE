upgrading from version 0.3 or below to 0.4:

An extra index was added to the events table. You can add this index to your database by loging in to the mysql console and issueing the command:

ALTER TABLE events ADD KEY stoptime (stoptime);
